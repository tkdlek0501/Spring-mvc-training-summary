package hello.itemservice.domain.product;

// 저장용 그룹 표시용 -> Product 객체에서 validation 어노테이션의 속성? group으로 지정할 수 있음 
// => but 복잡하기 때문에 이렇게 잘 안쓴다 차라리 등록시, 수정시 객체를 따로 나눠서 쓴다 
public interface SaveCheck {
}
